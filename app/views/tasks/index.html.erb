<h1>My tasks</h1>
  <% @tasks.each do |task| %>
    <p><%= link_to task.title, task_path(task) %></p> <%= link_to "Delete", task_path(task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
  <% end %>
<h3> <%= link_to "+ Add a new task", new_task_path %></h3>
